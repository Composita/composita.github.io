{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "serve: deploy",
            "dependsOn": [ "yarn: predeploy" ],
            "command": "serve",
            "args": [
                "-s",
                "build",
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "type": "shell",
            "label": "yarn: predeploy",
            "dependsOn": [ ],
            "command": "yarn",
            "args": [
                "predeploy",
            ],
            "group": "build"
        }
    ]
}
