import { default as React, Component } from 'react';

export class About extends Component {
    render(): JSX.Element {
        return (
            <div>
                <p>About</p>
            </div>
        );
    }
}
